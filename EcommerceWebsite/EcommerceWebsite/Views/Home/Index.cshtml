@{
    ViewBag.Title = "Index";
}

@model IEnumerable<Product>

<div style="padding-top:40px;margin-left:-20px;" class="container-fluid">
    <div class="row">
        @foreach (var item in Model)
        {
            <div class="col-md-4">
                <div class="thumbnail right-caption span4" style="width: 250px; height: 400px;border:none;">
                    <img src='@Url.Action("show", "image", new { id = item.Id })' width="250" height="400" />
                    <div class="caption">
                        <h3>@item.Name</h3>
                        <div class="price">
                            <label class="btn btn-primary btn-sm">@item.Price.ToString("c")</label>
                        </div>
                        @*<p class="prdtdesc"> @item.ShortDescription</p>*@
                        @*@using (Html.BeginForm("AddToCart", "Cart"))
                            {
                                <div>
                                    @Html.HiddenFor(x => x.Id)
                                    @Html.Hidden("returnUrl", Request.Url.PathAndQuery)
                                    <input type="submit" class="btn btn-success" value="Add to cart" />
                                </div>
                            }*@
                    </div>
                </div>
            </div>
        }
    </div>
</div>


@*
    <div style="padding-top:50px;"class="container-fluid">
        <div class="row">
            @foreach (var item in Model)
            {
                <div class="col-md-4">
                    <div class="products" style="overflow:hidden;">
                        <div class="picture">
                                <img src="@Url.Action("show", "Image", new { id = item.Id})" class="img-responsive" />
                            </div>
                        <div>
                            <h3 class="product-title">@item.Name</h3>
                            <div class=" prices">
                                <span class="label label-info">@item.Price.ToString("c")</span>
                            </div>
                            <span>@item.ShortDescription</span>
                        </div>
                    </div>
                </div>
            }
        </div>
    </div>
*@